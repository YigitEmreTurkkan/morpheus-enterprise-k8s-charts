apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mariadb-database.fullname" . }}-pvc
  labels:
    {{- include "mariadb-database.labels" . | nindent 4 }}
    component: database
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.database.storage.size }}

