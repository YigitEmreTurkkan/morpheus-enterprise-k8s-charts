apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "postfix-server.fullname" . }}-config
  labels:
    {{- include "postfix-server.labels" . | nindent 4 }}
data:
  postfix-accounts.cf: |
    admin@cloudflex.tr|{PLAIN}Admin123!
  postfix-virtual.cf: |
    # Virtual aliases (if needed)
  user-patches.sh: |
    #!/bin/bash
    # Custom user patches script
    echo "Postfix server initialized"


