apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "postgres-database.fullname" . }}-pvc
  labels:
    {{- include "postgres-database.labels" . | nindent 4 }}
    component: database
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.database.storage.size }}

