apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mssql-database.fullname" . }}-pvc
  labels:
    {{- include "mssql-database.labels" . | nindent 4 }}
    component: database
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.database.storage.size }}

